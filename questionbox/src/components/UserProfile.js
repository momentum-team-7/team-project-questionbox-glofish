import React, { useEffect, useState } from 'react'
import { getUserProfiles } from "../userApis.js"


export default function UserProfile({userprofile}) {
    //  const [grabProfile, setgrabProfile] = useState([])   

    // useEffect(() => {
    //     getUserProfiles(1).then((data) => setUserProfile(data))
    // }, [id])

    // return (
    //     <main>
    //          <h1>Profiles</h1>
    //         <p>{userprofile.id}</p>
    //         <p>{userprofile.username}</p>
    //         <p>{userprofile.date_created}</p>
    //         </main>
    //         )
    }